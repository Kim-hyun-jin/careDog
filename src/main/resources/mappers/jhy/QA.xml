<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.oracle.s20221103.jhy.QA">
	<select id="qaList" parameterType="String" resultType="QA">
		SELECT * FROM qa WHERE status = #{memberId}
	</select>
</mapper>